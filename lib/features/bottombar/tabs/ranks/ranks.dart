import 'package:flutter/material.dart';

class Ranks extends StatelessWidget {
  const Ranks({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("This is Ranks page"),
    );
  }
}